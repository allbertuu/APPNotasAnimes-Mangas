var notaFinal = notaAnimação * 2 + notaEnredo * 2 + notaRoteiro * 3 + notaAdaptação * 3;
// mostrar ao usuário
for (var nota = 0.5; nota <= 10; nota = nota + 0.5) {
	if (notaFinal < nota) {
		var notaFinal = ;
		"A nota final é " + notaFinal; 
}	
}